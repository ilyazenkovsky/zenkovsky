<div class="container">
  <div class="section">
    <div class="row">
      <div class="col s12 m2">
      </div>
      <div class="col s12 m8">
        <h5>Задача №<span id="taskId">{{id}}</span> - {{taskTitle}}</h5>
        </br>
        </br>
        <div class="row">
          <div class="row">

            <div class="col s8">
              <h6>{{taskDesc}}</h6>
            </div>
            <div class="col s4">

              <form id="estDate" class="col s12">
                <h7>Предполагаемое время выполнния:</h7>
                <input id="formDate" type="text" class="datepicker" dateFormat: "d M yy" value={{estCompletion}}>
                <button id=" btnSave" class="btn waves-effect waves-light" type="submit">Сохранить</button>
              </form>
            </div>
          </div>
        </div>
        </br>
        </br>
        <div class="row">
          <div class="col s5">Создана: {{creatorFirstName}} {{creatorLastName}}</div>
          <div class="col s5">Назначена на: {{assigneeFirstName}} {{assigneeLastName}}</div>
        </div>
        <div class="row">
          <div class="col s3">
            <button id="btnBack" class="btn waves-effect waves-light" type="submit">Назад
            </button>
          </div>
          <div class="col s4">
            <button id="btnAssign" class="btn waves-effect waves-light" type="submit">Назначить на меня
            </button>
          </div>
          <div class="col s3">
            <button id="btnComplete" class="btn waves-effect waves-light" type="submit">Выполнена
            </button>
          </div>
        </div>
        </br>
      </div>
      <div class="col s12 m2">
      </div>
    </div>
  </div>
</div>